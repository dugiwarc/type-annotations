// 'interface' for managing order in a tuple
type Drink = [string, boolean, number];

const pepsi: Drink = ['brown', true, 40];

const drinkStats = {
	color: 'brown',
	carbonated: true,
	sugar: 40
};
